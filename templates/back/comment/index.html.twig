{% extends 'layouts/' ~ data.layout ~ '.html.twig' %}

{% block title %}Comment index{% endblock %}

{% block main %}
<div class="container">
    <div class="d-flex flex-wrap justify-content-between gap-3 mb-3">
        <a class="btn btn-secondary text-nowrap" href="{{ path('app_back') }}"><i class="bi bi-arrow-return-left me-1"></i> back to dashboard</a>
        <div class="d-flex gap-2">
            <span class="btn btn-outline-dark disabled text-nowrap">countComments: {{ countComments }}</span>
            <a class="btn btn-success text-nowrap" href="{{ path('app_comment_new') }}">create new <i class="bi bi-plus-circle ms-1"></i></a>
        </div>
    </div>

    <div class="overflow-auto border rounded">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>CreatedAt</th>
                    <th>Content</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for comment in comments %}
                <tr>
                    <td>{{ comment.id }}</td>
                    <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    <td>{{ comment.content }}</td>
                    <td>
                        <a class="text-decoration-none text-secondary mx-1" href="{{ path('app_comment_show', {'id': comment.id}) }}">show <i class="bi bi-eye"></i></a>
                        <a class="text-decoration-none mx-1" href="{{ path('app_comment_edit', {'id': comment.id}) }}">edit <i class="bi bi-pencil"></i></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
