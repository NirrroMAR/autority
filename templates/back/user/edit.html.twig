{% extends 'layouts/' ~ data.layout ~ '.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block main %}
	<div class="container">
		<div class="d-flex justify-content-between flex-wrap gap-3 mb-3">
			<a class="btn btn-secondary text-nowrap" href="{{ path('app_user_index') }}"><i class="bi bi-arrow-return-left me-1"></i> back to list</a>
			<div class="d-flex gap-2">
				{{ include('back/user/_delete_form.html.twig') }}
				<a class="btn btn-outline-secondary text-nowrap" href="{{ path('app_user_show', {'id': user.id}) }}">show <i class="bi bi-eye"></i></a>
			</div>
		</div>
		{{ include('back/user/_form.html.twig', {'button_label': 'update'}) }}
	</div>
{% endblock %}
