{% extends 'layouts/' ~ data.layout ~ '.html.twig' %}

{% block title %}Edit Post
{% endblock %}

{% block main %}
	<div class="container">
		<div class="d-flex justify-content-between flex-wrap gap-3">
			<a class="btn btn-secondary text-nowrap" href="{{ path('app_post_index') }}"><i class="bi bi-arrow-return-left me-1"></i> back to list</a>
			<div class="d-flex gap-2">
				{{ include('back/post/_delete_form.html.twig') }}
				<a class="btn btn-outline-secondary text-nowrap" href="{{ path('app_post_show', {'id': post.id}) }}">show <i class="bi bi-eye"></i></a>
			</div>
		</div>
		{{ include('back/post/_form.html.twig', {'button_label': 'update'}) }}
	</div>
{% endblock %}
