{% extends 'layouts/' ~ data.layout ~ '.html.twig' %}

{% block title %}New Post{% endblock %}

{% block main %}
<div class="container">
    <a class="btn btn-secondary text-nowrap mb-3" href="{{ path('app_post_index') }}"><i class="bi bi-arrow-return-left me-1"></i> back to list</a>
    {{ include('back/post/_form.html.twig') }}
</div>
{% endblock %}
